import Post from '../../models/postModel.js';

export default function deletedPostController(req, res) {
    try {
        const { userID, description, mediaURL } = req.body;
        let media = null;

    if {
        
    }
}